import React from 'react';

const Subject = () => {
    const {logo, name} = topic
    return (
        <div>
            <p>{logo}</p>
            <p>{name}</p>
        </div>
    );
};

export default Subject;